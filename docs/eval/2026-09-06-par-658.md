# PAR-658 retrieval evaluation — legacy overlap ranker vs BM25

**Date:** 2026-09-06 · **Branch:** `build/par-658-bm25` · **Decision:** D-27
· **Re-run 2026-09-06 against the amended D-23 stemmer** (Porter step-1b repairs:
de-doubling and the silent-e cut). Every number below is from that re-run; the
legacy baseline is frozen in the script and did not move.

## Verdict, first

**BM25 did not beat the legacy ranker on this probe set. Both score 18/60 top-1
hits (18/23 of the answerable questions). MEASURED, not asserted.** The two
rankers disagree on the top result for 14 of the 60 questions, but every one of
those 14 is a question this corpus cannot answer, so BM25 gained 0 hits and lost
0. PAR-658's "correct section in top result improves over the current ranker"
done-when is **NOT met on this corpus**; see *What this does and does not show*.

**The amended stemmer did not flip any question either** — same 18 hits, same
five answerable misses. It changed the top-1 on two unanswerable questions
(`prisma / relation include`, `convex / schema define table`) and cost 16 tokens
of average response. What it fixed is real but invisible at top-1 here; the
`zod / parse object schema` case is dissected below.

## How the numbers were produced

- Script: `scripts/eval-retrieval.mjs`, run as `npm run build && node scripts/eval-retrieval.mjs`.
- Gold: `docs/eval/probe-gold.json` — 60 questions, being each of the 30 registry
  entries paired with each of its two `probeQueries`. Each question carries
  heading-path regexes for the section that would count as the correct top result,
  labelled by reading the fetched documents **before** either ranker was run over
  them, under the labelling rule stated in that file's header.
- Legacy baseline: the `tokenize` / `splitSections` / `rankSections` / `assemble`
  from `src/retrieval.ts` at 5b31f33, copied verbatim into the script so the
  baseline cannot drift with `src/`.
- Both rankers rank the **primary document only** — index links are not followed,
  so the two are compared on identical input.
- Response size is the rendered `assemble(ranked, 4000)` length divided by 4
  (VibeCTX's own approximate-token unit).

## Corpus provenance — read this before trusting the hit rates

Every one of the 30 documents in this run was the entry's
**`raw.githubusercontent.com` README fallback**. Not one docs-site
`llms-full.txt` / `llms.txt` primary was reachable from the build sandbox, which
has network access to `registry.npmjs.org`, `pypi.org` and
`raw.githubusercontent.com` only. The per-question table below records the
resolved URL class; the script prints each URL to stderr.

That matters enormously. A README is a fraction of an `llms-full.txt`: **37 of
the 60 probe queries have no correct answer anywhere in the document that was
fetched** (`expect: []` in the gold file, with a per-question reason). Prisma's
README never says "upsert"; the Stripe library README never says "checkout";
Tailwind's README is four sections of links. The probe queries were written
against the real docs sites, and the real docs sites are exactly what this
sandbox cannot see.

So these hit rates measure the two rankers **against a corpus neither was
designed for**. They are a valid A/B — same input, same gold — and an invalid
estimate of production quality in either direction.

## Results (MEASURED)

    
    library         query                      legacy top-1                                 L    BM25 top-1                                   B
    -----------------------------------------------------------------------------------------------------------------------------------------------
    next.js         server actions revalidate  Community                                    n/a  Community                                    n/a 
    next.js         app router layout          (no sections matched)                        n/a  (no sections matched)                        n/a 
    react           useEffect cleanup          Troubleshooting {/*troubleshooting*/} > My … HIT  Troubleshooting {/*troubleshooting*/} > My … HIT 
    react           context provider           (no sections matched)                        n/a  (no sections matched)                        n/a 
    supabase        row level security policy  Support Policy                               n/a  Support Policy > Deno                        n/a 
    supabase        auth sign in with oauth    Known Build Warnings > `UNUSED_EXTERNAL_IMP… n/a  (intro)                                      n/a 
    tailwindcss     dark mode variant          (intro)                                      n/a  (intro)                                      n/a 
    tailwindcss     responsive breakpoints     (no sections matched)                        n/a  (no sections matched)                        n/a 
    shadcn          add button component       Existing Project > Add Components            HIT  Existing Project > Add Components            HIT 
    shadcn          theming css variables      Existing Project > Configure Tailwind CSS a… n/a  Existing Project > Configure Tailwind CSS a… n/a 
    stripe          checkout session create    Stripe Node.js Library > Usage > Using Prom… n/a  Stripe Node.js Library > Usage > Using Prom… n/a 
    stripe          webhook signature verify   Stripe Node.js Library > Configuration > We… HIT  Stripe Node.js Library > Configuration > We… HIT 
    ai-sdk          streamText tool calling    AI SDK > Usage > UI Integration > UI Compon… miss AI SDK > Usage > UI Integration > Route (Ne… miss
    ai-sdk          useChat hook               AI SDK > Usage > UI Integration > Page @/ap… HIT  AI SDK > Usage > UI Integration > Page @/ap… HIT 
    expo            expo router navigation     The Expo ecosystem > Expo Router             HIT  The Expo ecosystem > Expo Router             HIT 
    expo            push notifications         The Expo ecosystem > Expo SDK                n/a  The Expo ecosystem > Expo SDK                n/a 
    drizzle-orm     select with where          What's Drizzle?                              n/a  What's Drizzle?                              n/a 
    drizzle-orm     migrations generate        Ecosystem                                    HIT  Ecosystem                                    HIT 
    prisma          upsert                     (no sections matched)                        n/a  (no sections matched)                        n/a 
    prisma          relation include           Getting started > 3. Use your AI agent for … n/a  For extension authors                        n/a 
    trpc            create router procedure    yarn                                         n/a  Quickstart                                   n/a 
    trpc            react query client         Intro > Features                             n/a  Intro > Features                             n/a 
    zod             parse object schema        Basic usage > AOT compilation                miss What is Zod?                                 miss
    zod             refine custom validation   Basic usage > Handling errors                n/a  Basic usage > Handling errors                n/a 
    hono            middleware                 Features                                     n/a  Features                                     n/a 
    hono            route params               (no sections matched)                        n/a  (no sections matched)                        n/a 
    bun             bun install                Configuration > Configuring a default port   n/a  Configuration > Configuring a default port   n/a 
    bun             Bun.serve http server      HTTP/2                                       miss HTTP/2                                       miss
    vite            env variables              (no sections matched)                        n/a  (no sections matched)                        n/a 
    vite            config proxy               Getting Started > `index.html` and Project … n/a  Getting Started > `index.html` and Project … n/a 
    clerk           middleware protect routes  Getting Started > Installation               n/a  Getting Started > Installation               n/a 
    clerk           useUser hook               (no sections matched)                        n/a  Getting Started                              n/a 
    convex          mutation query function    (no sections matched)                        n/a  Repository layout                            n/a 
    convex          schema define table        (no sections matched)                        n/a  Repository layout                            n/a 
    firebase        firestore query where      Firebase JavaScript SDK > Testing the SDK >… n/a  Firebase JavaScript SDK > Testing the SDK >… n/a 
    firebase        auth sign in google        Firebase JavaScript SDK > Testing the SDK >… n/a  Firebase JavaScript SDK > Testing the SDK >… n/a 
    openai          chat completions streaming OpenAI TypeScript and JavaScript API Librar… HIT  OpenAI TypeScript and JavaScript API Librar… HIT 
    openai          structured outputs         (no sections matched)                        n/a  OpenAI TypeScript and JavaScript API Librar… n/a 
    anthropic-sdk   streaming messages         <img src=".github/logo.svg" alt="" width="3… HIT  <img src=".github/logo.svg" alt="" width="3… HIT 
    anthropic-sdk   tool use                   (no sections matched)                        n/a  (no sections matched)                        n/a 
    playwright      locator click              🎭 Playwright > VS Code Extension            miss 🎭 Playwright > VS Code Extension            miss
    playwright      expect toBeVisible         🎭 Playwright > Playwright Test > Write a t… HIT  🎭 Playwright > Playwright Test > Write a t… HIT 
    vitest          mock function              Getting Started > Writing Tests              n/a  Getting Started > Writing Tests              n/a 
    vitest          config coverage            Getting Started > Configuring Vitest         HIT  Getting Started > Configuring Vitest         HIT 
    react-router    loader data                Routing > Route Modules                      HIT  Routing > Route Modules                      HIT 
    react-router    nested routes outlet       Routing > Nested Routes                      HIT  Routing > Nested Routes                      HIT 
    astro           content collections        Slots > Fallback Content for Slots           n/a  (intro)                                      n/a 
    astro           islands client directive   (intro)                                      miss (intro)                                      miss
    sveltekit       load function              +layout > +layout.js                         HIT  +layout > +layout.js                         HIT 
    sveltekit       form actions               +page > +page.server.js                      HIT  +page > +page.server.js                      HIT 
    nuxt            useFetch data fetching     Nuxt                                         n/a  Nuxt                                         n/a 
    nuxt            server api routes          Nuxt                                         n/a  Nuxt                                         n/a 
    vue             computed reactive ref      Introduction {#introduction} > API Styles {… HIT  Introduction {#introduction} > API Styles {… HIT 
    vue             component props emit       Introduction {#introduction} > API Styles {… n/a  Introduction {#introduction} > Single-File … n/a 
    tanstack-query  useQuery stale time        (no sections matched)                        n/a  TanStack Query > Explore the TanStack Ecosy… n/a 
    tanstack-query  mutation invalidate queri… TanStack Query                               n/a  TanStack Query                               n/a 
    motion          animate variants           🍦 Platforms > JS                            HIT  🍦 Platforms > JS                            HIT 
    motion          layout animation           Why Motion?                                  n/a  Why Motion?                                  n/a 
    resend          send email react template  Resend Node.js SDK > Send email using React  HIT  Resend Node.js SDK > Send email using React  HIT 
    resend          domains verify             Resend Node.js SDK > Usage                   HIT  Resend Node.js SDK > Usage                   HIT 
    

### Totals (MEASURED, `node scripts/eval-retrieval.mjs`, 2026-09-06, amended stemmer)

| metric | legacy | BM25 |
| --- | --- | --- |
| top-1 hits, all questions | **18/60 (30.0%)** | **18/60 (30.0%)** |
| top-1 hits, answerable subset | **18/23 (78.3%)** | **18/23 (78.3%)** |
| "No sections matched" responses | 12/60 | 7/60 |
| avg rendered response | 861 tokens | 976 tokens |
| top-1 changed | — | 14/60 questions |
| hits gained / lost by BM25 | — | +0 / −0 |
| questions with ≥1 matching code block (`mode: "snippets"`) | — | 35/60 |

What the stemmer amendment moved, against the same run before it (BM25 column
only; legacy is frozen): hits 18 → 18, "no sections matched" 8 → 7, avg response
960 → 976 tokens, top-1 changed 12 → 14, snippet coverage 35/60 → 35/60.

## What this does and does not show

**Even.** Neither ranker wins a single question the other loses. On the 23
answerable questions both pick the same section 18 times, and both miss the same
five: `ai-sdk / streamText tool calling`, `zod / parse object schema`,
`bun / Bun.serve http server`, `playwright / locator click`,
`astro / islands client directive`.

**BM25 answers more often (12 → 7 "no sections matched").** The new tokenizer's
camelCase splitting is what does most of it: `useUser hook` tokenizes to
`use, user, useuser, hook`, and `use` occurs in almost any document, so clerk's
README now returns a section where the legacy ranker correctly returned nothing.
The amended stemmer adds one more, and it is a fair illustration of the trade:
`convex / schema define table` now tokenizes to `schema, defin, tabl`, and
`defin` matches `user-defined` in "Repository layout" — a real morphological
match to a section that still does not answer the question. Recall up, precision unchanged
**on this corpus** — and on a README the extra recall is noise. On an
`llms-full.txt`, where the answer usually does exist, the same behaviour is the
point of the change; that is a hypothesis this run cannot test.

**Responses got 13% longer (861 → 976 tokens).** Same cause: BM25 admits more
sections above zero, and `assemble` fills the 4,000-token budget with them. The
stemmer amendment accounts for 16 of those 115 tokens.

**The stemmer defect is fixed, and it did not flip the question it was blamed
for.** Before the amendment, `tokenize("parse")` → `["parse"]` while
`tokenize("parsing")` → `["pars"]`: `ing` came off without the silent `e`, so the
two never met. They now both stem to `pars`, together with `parsed` and `parses`
(MEASURED — `test/tokenize.test.ts` "stems inflections of the same word to one
token", eleven families).

**`zod / parse object schema` still misses, and the earlier note overstated the
stemmer's part in it.** MEASURED on the same cached document, top-1 before → after
the amendment: "What is Zod?" 2.527 → 2.678 stays first; the gold section
"Basic usage > Parsing data" gains from the repair, 1.986 → 2.143, but was third
before and is third now, behind "Basic usage > Handling errors" (2.273 → 2.329)
and "Basic usage > AOT compilation" (2.271 → 2.271). Term counts for the three
query terms `pars, object, schema`, MEASURED over the same tokenizer: the gold
section carries 5 / 0 / 4 in 79 tokens; "Handling errors" 7 / 1 / 3 in 182;
"AOT compilation" 2 / 3 / 11 in 194; and the winner "What is Zod?" 3 / 1 / 1 in
54. Each rival beats the gold section on a term it lacks or barely has, and the
short intro wins on density — a top-1 ordering problem, not a tokenizer problem.
The remaining five answerable misses are unchanged.

**Two non-convergences are deliberate and documented in `src/tokenize.ts`:**
`using` (a stopword; alone, `ing` → `us` is under the three-character floor) and
`handler` (no agent-noun rule — an `er` rule would also merge `router` into
`route`). The silent-e cut over-stems a few nouns, `stripe` → `strip` and
`middleware` → `middlewar` among them; queries and documents run through the same
function, so those cost precision, never a match.

**`mode: "snippets"` has something to return on 35 of the 60 questions** — that is
a capability count on this corpus, not a quality measurement. No gold set exists
for snippets yet.

## Live snippet checks (build item 5, MEASURED)

Both run from the build sandbox against the same README fallbacks.

| call | result |
| --- | --- |
| `getDocs(stripe, "checkout session create", mode: "snippets")` | source `raw.githubusercontent.com/stripe/stripe-node/master/README.md`; **9 snippets matched**; top snippet heading path `Stripe Node.js Library > Usage > Usage with TypeScript`, first code line `import Stripe from 'stripe';` |
| `getDocs(prisma, "upsert", mode: "snippets")` | source `raw.githubusercontent.com/prisma/prisma/main/README.md`; **0 snippets matched** → `No code snippets matched "upsert" in prisma docs (source: …). Try mode "sections" or broader terms.` |

Neither returns the call PAR-658 names, because neither call is in the README the
sandbox can reach: `stripe.checkout.sessions.create` lives in
`docs.stripe.com/llms-full.txt` and `prisma.user.upsert` in
`prisma.io/docs/llms-full.txt`, both unreachable here. **The done-when
"`mode: "snippets"` returns runnable code for 'stripe checkout session create'
and 'prisma upsert'" is NOT VERIFIED against the real docs sites.** The mechanism
is proven instead by unit tests over fixture markdown shaped like those pages —
`test/retrieval.test.ts` "returns the Stripe checkout-session call for 'stripe
checkout session create'" and "returns the Prisma upsert call for 'prisma
upsert'", plus `test/get-docs.test.ts` "returns the fenced code block with its
heading path and context line, and the Source line".

## Reproducing

```
npm run build
node scripts/eval-retrieval.mjs
```

The script caches fetched documents under `$TMPDIR/vibectx-eval-cache` (override
with `DOCS_CACHE_DIR`), so a second run is offline and byte-identical. Run it
from a machine with unrestricted network access to get the numbers that matter:
the same 60 questions against the `llms-full.txt` primaries. Those numbers do not
exist yet and are not claimed here.

## Standards applied

No published standard governs retrieval evaluation for this use. The practice
applied is a fixed, pre-labelled top-1 relevance set with a frozen baseline
implementation, scored by exact A/B on identical input — the gold set was written
from the documents before either ranker was run and was not adjusted afterward.
