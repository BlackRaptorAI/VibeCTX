# Change Record — CR-20260917-release-0.2.0

Per the lean process (D-70): one Change Record per release, not one per item. Individual
0.2.0 items each merged through their own PR, reviewed by `code-reviewer` (and
`security-architect` where the change touched URL/host policy, fetching, redirects, config
URL handling, or the on-disk cache/search index) — see each PR for that evidence. This record
is the release-level summary: what 0.2.0 actually is, what was verified against real data
before shipping, and the risks accepted rather than fixed.

## 1. Change summary

0.2.0 is the GA release covering the 2026-09-08 audit remediation batch (the `PAR-7xx` items
under the 0.2.0 milestone) plus A11 (version-matched documentation, PAR-724) and A16 (the
package-existence signal, PAR-725). `package.json` is bumped to `0.2.0` in this same PR, built
on `main` @ `562037b` (after merging two PRs that landed on `main` after this branch was first
cut: PAR-832/D-81's clerk fix and PAR-832a's content negotiation — see §4/§5); this record
still precedes the tag, which Tom cuts after reviewing and signing §7 below.

**What 0.2.0 promises, and doesn't:** version-matched documentation with a stated, non-silent
fallback when no pinned-version document exists (D-50, A11); the package-existence signal
(A16); provenance stamping on every response (A17); honest gap statements when nothing is
cached or nothing matches (A18); a redirect-aware primary document resolves its own relative
links and host policy against the host it was actually served from, not the pre-redirect
candidate (PAR-776, D-74); PEP 503 name-twin handling in the registry (PAR-777, D-78); the
Node engine floor now states the true intersection of `vite`'s and `vitest`'s own declared
ranges, enforced by a dedicated test rather than hand-maintained, and a CI matrix that runs
one version per declared band (PAR-778, PAR-779, PAR-829); the two moderate `vitest`-chain
advisories present at the earlier release-verification pass are resolved, not accepted
(PAR-830). **0.2.0 makes no retrieval-quality claim** — see §5.

## 4. Mechanical evidence

CI is the record of test results per PR (not restated here as a baseline number, per the lean
process) — each merged PR's own CI run is the evidence for that change. The figures below are
the release-level snapshot, re-measured on this release branch after merging `main`'s two
subsequent PRs (PAR-832/D-81 clerk fix, PAR-832a content negotiation).

MEASURED 2026-09-18, Node v26.0.0 (`node -v`; local verification, inside the unbounded top band
the manifest declares — see the engines note below):

- **Test suite:** 1,636 tests across 45 files, all passing (`npm ci && npm test`) — six more
  than the 1,630 at the prior measurement in this same record, added by PAR-832a's fetcher and
  registry tests.
- **`npm audit` on the full tree — not `--omit=dev`:** 0 vulnerabilities. The two moderate
  `vitest`-chain advisories reported at the 2026-09-17 release-verification pass are RESOLVED
  (PAR-830: `vitest` bumped to `4.1.11`, the minimal patched release), not accepted.
- `npm run lint` (`tsc --noEmit`) and `npm run build` both clean.
- **`engines.node` is `^20.19.0 || ^22.12.0 || >=24.0.0`** — the true intersection of `vite`'s
  and `vitest`'s own declared ranges. The value is still hand-maintained in `package.json`, but
  `test/engines.test.ts` (via `semver`, a new devDependency) checks it at test time against
  both dependencies' actual installed ranges, so it can no longer silently drift out of sync
  with what they require. CI's `test-matrix` (PR #26) ran one version per declared band:
  `20.19.6`, `22.23.2`, `24.20.0` — all green. That is not the same claim as "the floor is
  proven across the range": the top band (`>=24.0.0`) is unbounded and always will be, so no
  CI run can prove it exhaustively; one representative version per band is what was, and can
  be, tested.

## 5. Deviations & risk acceptance

> **Upgrading? Read this before concluding the release is broken.** An existing install will
> **not** see the clerk fix or the content-negotiation fixes below until the affected cache
> entry's TTL expires. Cache entries are keyed per-URL, and `getLibraryDoc` (`src/fetcher.ts`)
> reads cache-first, walking a library's candidate URLs in order and returning the first fresh
> hit before ever reaching the network — a fresh cached copy of the OLD (broken) result is
> served exactly as confidently as a fresh copy of the new one. **`vibectx warm --force` does
> NOT clear this** — it only bypasses a recent resolution-*failure* memo, never the document
> cache itself (verified: run against a deliberately poisoned cache, no change). What does
> work: deleting the affected library's cache directory, waiting out the TTL (168h default), or
> the MCP `refresh` tool (`src/refresh.ts`, which calls `getLibraryDoc` with
> `forceRefresh: true`, skipping the cache-first loop entirely). Someone who upgrades, runs
> `doctor`, and still sees `clerk` unhealthy has not found a broken 0.2.0 — they have found
> this gap. Tracked as **PAR-836** (no milestone assigned yet; the general defect will recur on
> any future curated-URL reorder, not just this one).

| What | Evidence | Decision | Why acceptable | Revisit by |
|---|---|---|---|---|
| **Retrieval quality on the real docs-site corpora remains UNVALIDATED — PAR-658's ranking improvement shipped but was never measured.** `scripts/eval-retrieval.mjs`, run from a Mac against the real corpus for the first time (2026-09-17, `main` @ `6de98f8`): legacy top-1 4/60 (6.7%), BM25 top-1 3/60 (5.0%), 23/60 questions marked answerable, 29/60 top-1 answers changed by BM25. These numbers do not measure ranker quality: `docs/eval/probe-gold.json`'s labels were hand-written against GitHub README fallbacks (the docs-site documents were unreachable from the build sandbox at label time); the corpus in this run is the real docs-site `llms-full.txt` documents (Prisma 5.98 MB, OpenAI 4.77 MB, Nuxt 4.79 MB, and others) — different documents than the labels describe. A correct answer drawn from an `llms-full.txt` section cannot match a label written against a README heading path except by accident. | MEASURED 2026-09-17 on PAR-653 (Linear comment). Gold-set provenance metadata (`docs/eval/probe-gold.json`) confirms the label/corpus mismatch. | **ACCEPT-WITH-RISK** — ship 0.2.0 making no retrieval-quality claim rather than block the release on re-labelling. | Re-verified 2026-09-18 on this branch, post-merge: `grep -rniE "better (ranking\|retrieval\|results)\|improve[sd]? (ranking\|retrieval\|relevance)\|more relevant" README.md docs/` returns nothing — 0.2.0's text makes no such claim. PAR-658's done-when stays **unmet** and is accepted as such for this release. **PAR-827** (0.2.1 milestone) now carries both halves of the fix: re-label all 60 probe questions against the real docs-site documents and re-run, **and** extend the eval validator to refuse to produce a score when a resolved URL disagrees with the corpus its labels declare — added to PAR-827's scope 2026-09-18 so a future re-divergence fails loudly instead of printing a meaningless number again. |
| **28/30 measures "returned a section above threshold," NOT "returned the right section" — the true corrected coverage figure is lower and not yet known.** `vibectx doctor --json`, fresh cache, real network, MEASURED 2026-09-18T17:46:17.810Z on this branch (post-merge, includes D-81 and D-82): 28 of 30 libraries returned a section scoring above `doctor`'s ranking threshold for their registered probe, up from a 24/30 baseline. `doctor` never checks whether that section actually answers the question — it only checks the score. Spot-checking three of the 28 on 2026-09-18 found one clean pass, one partial, one outright false positive: `prisma`/`"upsert"` returns precise, on-topic content (the `.upsert()` API reference); `supabase`/`"row level security policy"` returns real Supabase security documentation but general platform posture rather than RLS policy syntax — topically reasonable, not precise; `next.js`/`"server actions revalidate"` returns next.js's own **blog post listing** (release announcements, a "1,500 GitHub issues" retrospective) and is reported healthy, because the index document's own "Blog" section scored above threshold on generic overlap and link-following into real Server Actions content never triggered. That failure mode is specific to index-only sources, which have an index-vs-follow step for noise to win in; a full-text source like `prisma` has none. | MEASURED 2026-09-18T17:46:17.810Z (the mechanical 28/30 score), plus the three spot-checks above, both filed in full on **PAR-844** (2026-09-18). PAR-844 traces this to a queued-but-never-built half of PAR-728 (A19): PAR-728's own description already named "a required-term / overlap threshold per probe" as follow-up work, which shipped the classification surfacing but not the threshold itself. | **ACCEPT-WITH-RISK** — ship 0.2.0 with **no coverage claim beyond this wording**: 28/30 means retrieval returned something for 28 libraries, not that it returned the right thing (grep-verified: no coverage figure is stated as a current claim outside this record; `README.md:1181`'s `23/30 libraries healthy` is inside a `vibectx doctor` sample-output block, explicitly dated 2026-09-06 and captioned as a historical worked example from a sandbox that could only reach GitHub READMEs — not a claim about the shipped registry today. That sample is now itself stale in a second way this record's own spot-checks expose: it shows `✗ prisma: no match: "upsert"`, while PAR-844 finds `prisma`/`"upsert"` returns precise, on-topic content today. Filed as **PAR-846**, not blocking this release.). Blocking the release on PAR-844 was rejected as disproportionate to a release that already makes no external retrieval-quality claim (§5's retrieval-quality row above); the honest fix is a corrected measurement, not a delay. | **PAR-844** (0.2.1 milestone) — give `doctor` a topical-fit check (a required-term/overlap threshold, refusing an index document's own sections as an index-only source's answer, or a third `thin`/`unverified` state distinct from `answered`), re-run the full top-30, and record the corrected figure — **expected to go down**, and the record's qualifier should be replaced by that real figure once it exists, not left as 28/30 unqualified. |
| **`shadcn` remains index-only (28/30's first gap) — deferred on evidence, not overlooked.** Its site does not negotiate on `Accept` at all and serves markdown only at a `.md`-suffixed path (per PAR-838: verified live against the real site — 3 of shadcn's own candidate links confirmed the pattern; distinct from `doctor`'s own probe-level `0/5` followed/dropped count in the table above). A `.md`-suffix retry that would close this was built and taken through two independent adversarial reviews (`code-reviewer`, `security-architect`), both of which reproduced the *same* defect by different routes: the retry's loop-termination check tested `url.endsWith(".md")` against the whole `href` rather than the URL's pathname, so any followed link carrying a query string or fragment never satisfies the guard after mutation (`guide?v=1` → `guide.md?v=1`, which does not end in `.md` → `guide.md.md?v=1` → unbounded), bounded only by the origin eventually refusing an absurdly long path. Per PAR-838, `code-reviewer` found 31 such query-string-or-fragment links live in `hono`'s own cached index at the time of that review (a snapshot, not necessarily this exact count today — the site's own content can change), and mutation-tested the branch's own suite: 1,637 tests passed identically with the bug present and with it fixed — nothing discriminated it. | Findings recorded in full on **PAR-838** (0.2.1 milestone), carried forward from the two reviews so the analysis survives the session that produced it. | **ACCEPT-WITH-RISK, DEFERRED** — shipping the retry as reviewed risked an unbounded-request hang, and on an HTML-catch-all origin could have disabled the retry's own HTML-rejection guard by construction (serving raw HTML as documentation, `doctor` falsely reporting it healthy) — a regression, not merely a missed fix. Shipping the negotiation half alone (which does fix `next.js`/`hono`/`motion`) without the broken retry is strictly safer than shipping either both or neither. | **PAR-838** (0.2.1) — the roughly eight-line fix both reviewers converged on (thread an explicit `isMdRetry` flag instead of inferring it from a URL suffix, add an explicit `rejectHtml` option so the HTML guard can't be laundered by URL construction, restore the parent's stale-cache fallback on the retry path), plus fixtures for a query-string and a fragment link against an always-HTML origin, each asserting exactly two requests — mutation-tested (break it, confirm red) before it ships. |
| **`tailwindcss` is now diagnosed — a genuine COVERAGE gap, not retrieval or a probe-wording problem.** Both of the registry entry's own configured primary candidates 404: `https://tailwindcss.com/llms-full.txt` and `https://tailwindcss.com/llms.txt` (verified live, MEASURED 2026-09-18: both `404`). The entry falls through to its third candidate, `raw.githubusercontent.com/.../README.md` — a 1,838-byte marketing stub (verified live, exact byte count) containing a logo, badges, a tagline, and Community/Contributing sections, with a single line under its own "Documentation" heading reading "For full documentation, visit tailwindcss.com" — zero actual documentation content. Its `"responsive breakpoints"` probe correctly finds nothing there; the entry's *other* probe, `"dark mode variant"`, is reported `answered` against this same stub (verified live, MEASURED 2026-09-18) despite the stub never mentioning dark mode at all — itself a PAR-844-shaped false positive (a score above threshold, not a real answer), not evidence the stub has any real content. Tailwind's real documentation is not missing: `https://tailwindcss.com/docs/responsive-design` is live and reachable (verified, `200`) — the registry entry simply points at files that don't exist instead of the real docs path. | **PAR-839**, diagnosis completed 2026-09-18 following its own investigate-before-fixing plan; moved to the **0.2.1 milestone** (was filed into 0.2.0 while the cause was still unknown — its own text: "if the investigation shows the fix is large, move it to 0.2.1... but the finding itself belongs on the record now"). | **ACCEPT** — the finding is recorded now, as PAR-839 itself required, rather than shipping unowned; the fix (pointing the registry at a working URL) is a one-line registry change but still needs verification against the real content before it ships, which is what 0.2.1 provides room for. | **PAR-839** (0.2.1) — point `tailwindcss`'s registry entry at a URL that actually serves its documentation (candidates to verify: `tailwindcss.com/docs/responsive-design` itself, or a working docs-wide `llms.txt`/`llms-full.txt` path if Tailwind publishes one elsewhere), confirm the "responsive breakpoints" probe returns real content, and re-run the full top-30 to confirm no regression. |
| **The Node floor is now the true intersection of the toolchain's own declared requirements, and every declared band has run green in CI.** `package.json`'s `engines.node` (originally `>=20.19.0`, D-75/PAR-778) had never been exercised at that version, and a first correction against `vite` alone (`^20.19.0 \|\| >=22.12.0`) proved insufficient once `vitest`'s own range (bumped to 4.1.11 by PAR-830) revealed a narrower requirement with its own gap ("the Node 23 hole"). | **FIXED (R-1/PAR-829).** `engines.node` is `^20.19.0 \|\| ^22.12.0 \|\| >=24.0.0` — the intersection of `vite`'s and `vitest`'s own declared ranges — still hand-maintained in `package.json`, but checked at test time by `test/engines.test.ts` (via `semver`, a new devDependency) against each package's own installed range, so it can no longer silently drift out of sync with what they require. CI's `test-matrix` (PR #26) ran **one version tested per declared band**: `20.19.6`, `22.23.2`, `24.20.0`, all green, 45 files / 1,630 tests each leg, clean `npm ci`/lint/build. This is a narrower claim than "the floor is proven across the range" — the top band (`>=24.0.0`) is unbounded and no CI run proves it exhaustively; the excluded bands (21.x, 22.0.0–22.11.x, 23.x) remain deliberately untested, since nothing supported lives in them. | **FIXED AND MEASURED — closed.** `engine-strict` is still not set (no `.npmrc` here), so `npm ci` on an excluded version still only warns (`EBADENGINE`) rather than failing; the fix is manifest accuracy backed by a matching CI matrix, not install-time enforcement. Full round-by-round history (four code-reviewer rounds against the now-abandoned `release-0.2.0` branch, superseded here) is in D-80, `DECISIONS.md`. | None forced — the three-band CI matrix is the standing check; it fails loudly (per `test/engines.test.ts`'s own subset assertion) if `vite` or `vitest` ever declare an incompatible range again. |
| **Two narrow hardening gaps in the `Source:` stamp's URL handling (PAR-811), both non-blocking.** `stripStampQuery` fails OPEN on a URL-parse failure (returns the original, unstripped string) rather than closed, and does not strip userinfo (`user:pass@host`) the way the precedent it mirrors (`sanitizeLoggedUrl`) does; separately, its `new URL().href` round-trip silently normalizes the URL (case, punycode, `%`-encoding, a redundant default port), a side effect neither documented nor tested. | Found by `code-reviewer` and `security-architect` on PAR-811's review, filed as **PAR-816** and **PAR-818**; both remain open, narrowed to a small self-contained fix each — see their Linear comments. | **ACCEPT** — both gaps are unreachable today: every producer of the affected URL (`link-policy.ts`'s `validateLibraryUrl`/`sanitizeRemoteUrl`, `resolved-store.ts`'s `sanitizeRemoteUrl`) already validates it through the same WHATWG parser before it can reach `stripStampQuery`, so the render path's safety currently rests on an upstream invariant it doesn't itself enforce or test — a robustness gap, not a live exposure. | PAR-816, PAR-818 (0.2.1 milestone) — each a two-line fix; no date forced. |
| **`@blackraptorai/vibectx@0.1.2` remains published and tagged `latest` on npm**, predating the 0.1.3 fixes for a redirect-escape SSRF, a ReDoS link regex and unbounded response bodies. The npm account that owns it is no longer accessible, so it cannot be deprecated or unpublished from this project. | **PAR-781.** README and CONTRIBUTING both already state not to install from npm; this repository is the only current source. | **ACCEPT** — outside this release's (or this project's) ability to fix directly; source-only distribution is the mitigation already in place. | PAR-781 — recover the npm account, or get npm support to deprecate/unpublish 0.1.2 as unmaintained-with-known-vulnerabilities; no date forced, since resolution depends on npm support, not this codebase. |

### Full doctor table — BASELINE, MEASURED 2026-09-17T22:54:29.291Z, `main` @ `6de98f8`, cache `/Users/blackraptorai/.vibectx` (from PAR-653)

```
library         kind        cache  probe                                   links  mark
next.js         index-only  0.0h   2 probes: 2 answered                    0/3    ✗
react           index-only  0.0h   2 probes: 2 index-followed              6/0    ✓
supabase        index-only  0.0h   2 probes: 2 index-followed              2/0    ✓
tailwindcss     readme      0.0h   2 probes: 1 answered, 1 no match        0/0    ✗
shadcn          index-only  0.0h   2 probes: 2 answered                    0/5    ✗
stripe          index-only  0.0h   2 probes: 2 index-followed              10/1   ✓
ai-sdk          full-text   0.0h   2 probes: 2 answered                    0/0    ✓
expo            index-only  0.0h   2 probes: 2 index-followed              10/0   ✓
drizzle-orm     full-text   0.0h   2 probes: 2 answered                    0/0    ✓
prisma          full-text   0.0h   2 probes: 2 answered                    0/0    ✓
trpc            full-text   0.0h   2 probes: 2 answered                    0/0    ✓
zod             full-text   0.0h   2 probes: 2 answered                    0/0    ✓
hono            index-only  0.0h   2 probes: 2 answered                    0/52   ✗
bun             full-text   0.0h   2 probes: 2 answered                    0/0    ✓
vite            full-text   0.0h   2 probes: 2 answered                    0/0    ✓
clerk           index-only  0.0h   2 probes: 2 no match                    0/0    ✗
convex          index-only  0.0h   2 probes: 1 answered, 1 index-followed  10/88  ✓
firebase        readme      0.0h   2 probes: 2 answered                    0/0    ✓
openai          full-text   0.0h   2 probes: 2 answered                    0/0    ✓
anthropic-sdk   index-only  0.0h   2 probes: 2 index-followed              10/0   ✓
playwright      readme      0.0h   2 probes: 2 answered                    0/0    ✓
vitest          full-text   0.0h   2 probes: 2 answered                    0/0    ✓
react-router    readme      0.0h   2 probes: 2 answered                    0/0    ✓
astro           readme      0.0h   2 probes: 2 answered                    0/0    ✓
sveltekit       full-text   0.0h   2 probes: 2 answered                    0/0    ✓
nuxt            full-text   0.0h   2 probes: 2 answered                    0/0    ✓
vue             full-text   0.0h   2 probes: 2 answered                    0/0    ✓
tanstack-query  index-only  0.0h   2 probes: 2 index-followed              2/0    ✓
motion          index-only  0.0h   2 probes: 2 answered                    0/10   ✗
resend          full-text   0.0h   2 probes: 2 answered                    0/0    ✓

24/30 libraries healthy
✗ next.js: index-only, no links followed (answered from the link list at best)
✗ tailwindcss: no match: "responsive breakpoints"
✗ shadcn: index-only, no links followed (answered from the link list at best)
✗ hono: index-only, no links followed (answered from the link list at best)
✗ clerk: index-only, no links followed (answered from the link list at best); no match: "middleware protect routes"; no match: "useUser hook"
✗ motion: index-only, no links followed (answered from the link list at best)
```

**Reading of it (from PAR-653):** five of the six failures share one shape — an index-only
primary where zero links were followed while links were available to follow (`hono` skipped
52, `motion` 10, `shadcn` 5, `next.js` 3). Those libraries answer from a link list rather than
prose; that is the PAR-704 failure class, and `doctor` now marks it ✗ rather than reporting
healthy. Why those specific links are skipped is not diagnosed in this record.

### Full doctor table — CURRENT, MEASURED 2026-09-18T17:46:17.810Z, this branch (post-merge, D-81 + D-82), fresh cache, real network

```
library         kind        cache  probe                                    links   mark
ai-sdk          full-text   0.0h   2 probes: answered, answered             0/0     ✓
anthropic-sdk   index-only  0.0h   2 probes: index-followed, index-followed 10/0    ✓
astro           readme      0.0h   2 probes: answered, answered             0/0     ✓
bun             full-text   0.0h   2 probes: answered, answered             0/0     ✓
clerk           index-only  0.0h   2 probes: index-followed, index-followed 10/0    ✓
convex          index-only  0.0h   2 probes: answered, index-followed       10/88   ✓
drizzle-orm     full-text   0.0h   2 probes: answered, answered             0/0     ✓
expo            index-only  0.0h   2 probes: index-followed, index-followed 10/0    ✓
firebase        readme      0.0h   2 probes: answered, answered             0/0     ✓
hono            index-only  0.0h   2 probes: answered, answered             9/43    ✓
motion          index-only  0.0h   2 probes: index-followed, index-followed 10/0    ✓
next.js         index-only  0.0h   2 probes: answered, index-followed       1/2     ✓
nuxt            full-text   0.0h   2 probes: answered, answered             0/0     ✓
openai          full-text   0.0h   2 probes: answered, answered             0/0     ✓
playwright      readme      0.0h   2 probes: answered, answered             0/0     ✓
prisma          full-text   0.0h   2 probes: answered, answered             0/0     ✓
react           index-only  0.0h   2 probes: index-followed, index-followed 6/0     ✓
react-router    readme      0.0h   2 probes: answered, answered             0/0     ✓
resend          full-text   0.0h   2 probes: answered, answered             0/0     ✓
shadcn          index-only  0.0h   2 probes: answered, answered             0/5     ✗
stripe          index-only  0.0h   2 probes: index-followed, index-followed 10/1    ✓
supabase        index-only  0.0h   2 probes: index-followed, index-followed 2/0     ✓
sveltekit       full-text   0.0h   2 probes: answered, answered             0/0     ✓
tailwindcss     readme      0.0h   2 probes: answered, no match             0/0     ✗
tanstack-query  index-only  0.0h   2 probes: index-followed, index-followed 2/0     ✓
trpc            full-text   0.0h   2 probes: answered, answered             0/0     ✓
vite            full-text   0.0h   2 probes: answered, answered             0/0     ✓
vitest          full-text   0.0h   2 probes: answered, answered             0/0     ✓
vue             full-text   0.0h   2 probes: answered, answered             0/0     ✓
zod             full-text   0.0h   2 probes: answered, answered             0/0     ✓

28/30 libraries healthy
✗ shadcn: index-only, no links followed (answered from the link list at best)
✗ tailwindcss: no match: "responsive breakpoints"
```

**Reading of it:** `clerk` and `next.js`/`hono`/`motion` moved from ✗ to ✓ relative to the
baseline above — `clerk` via D-81's URL reorder, the other three via D-82's `Accept`-header
negotiation on followed links. `next.js`'s `1/2` followed/dropped is not `3/3`: its `/learn/*`
tutorial pages don't negotiate and stay `unavailable` (a real gap in what next.js's own site
serves as markdown, not a fetch defect). `shadcn` and `tailwindcss` are unchanged from the
baseline — see their dedicated rows above (PAR-838, PAR-839) for why each remains open and who
owns it. **A ✓ in this table means a section scored above `doctor`'s ranking threshold, not
that it answered the question** (PAR-844) — `next.js`'s own ✓ above is exactly the case where
that gap is real: its probe scores from the index document's blog listing, not from Server
Actions content. Do not read this table as 28 libraries confirmed correct.

## What DID verify against real data (2026-09-17, from PAR-653)

- **`vibectx warm` resolved 10 of 10 unfamiliar dependencies in a real project**
  (`/Volumes/BlackRaptorAI_PROJECTS/AA_PARAGON_PLATFORM`, manifest `package.json`) — none of the
  eleven dependencies checked were registry entries, so every one went through
  `resolve_library`; 10 resolved and cached, 1 denied by the noise list
  (`@types/html-to-text`).
- **A11's version fallback (D-50) was observed live, not just in a fixture.** Two of the ten
  resolutions hit the "no versioned document found" path and fell back to latest, stating so
  non-silently: `pdf-parse: no versioned document found for 1.1.1; latest cached instead` and
  `@biomejs/biome: no versioned document found for 2.5.14; latest cached instead`. This is
  D-50's rule ("the fallback to latest is always stated, never silent") behaving exactly as
  specified, on a real manifest.

## 7. Sign-off

> I ran the applicable gate reviews (per PR) and take responsibility for the risk acceptances
> recorded above.

**Signed:** Tom Hanks  **Date:** 2026-09-18
