# Change Record — CR-20260917-release-0.2.0

Per the lean process (D-70): one Change Record per release, not one per item. Individual
0.2.0 items each merged through their own PR, reviewed by `code-reviewer` (and
`security-architect` where the change touched URL/host policy, fetching, redirects, config
URL handling, or the on-disk cache/search index) — see each PR for that evidence. This record
is the release-level summary: what 0.2.0 actually is, what was verified against real data
before shipping, and the risks accepted rather than fixed.

## 1. Change summary

0.2.0 is the GA release covering the 2026-09-08 audit remediation batch (the `PAR-7xx` items
under the 0.2.0 milestone) plus A11 (version-matched documentation, PAR-724) and A16 (the
package-existence signal, PAR-725). `package.json` is bumped to `0.2.0` in this same PR,
built on `main` @ `032549b`; this record still precedes the tag, which Tom cuts after
reviewing and signing §7 below.

**What 0.2.0 promises, and doesn't:** version-matched documentation with a stated, non-silent
fallback when no pinned-version document exists (D-50, A11); the package-existence signal
(A16); provenance stamping on every response (A17); honest gap statements when nothing is
cached or nothing matches (A18); a redirect-aware primary document resolves its own relative
links and host policy against the host it was actually served from, not the pre-redirect
candidate (PAR-776, D-74); PEP 503 name-twin handling in the registry (PAR-777, D-78); the
Node engine floor and CI Action pins now state what they actually require (PAR-778, PAR-779).
**0.2.0 makes no retrieval-quality claim** — see §5.

## 4. Mechanical evidence

CI is the record of test results per PR (not restated here as a baseline number, per the lean
process) — each merged PR's own CI run is the evidence for that change. The figures below are
the release-level snapshot taken once, at the commit this record's version bump is built on.

MEASURED 2026-09-17, `main` @ `032549b`, Node v26.0.0:

- **Test suite:** 1,626 tests across 44 files, all passing (`npm test`).
- **`npm audit --omit=dev`:** 0 vulnerabilities.
- `npm run lint` (`tsc --noEmit`) and `npm run build` both clean.

## 5. Deviations & risk acceptance

| What | Evidence | Decision | Why acceptable | Revisit by |
|---|---|---|---|---|
| **Retrieval quality on the real docs-site corpora is UNVALIDATED.** `scripts/eval-retrieval.mjs`, run from a Mac against the real corpus for the first time (2026-09-17, `main` @ `6de98f8`): legacy top-1 4/60 (6.7%), BM25 top-1 3/60 (5.0%), 23/60 questions marked answerable, 29/60 top-1 answers changed by BM25. These numbers do not measure ranker quality: `docs/eval/probe-gold.json` was hand-labelled on 2026-09-06 against GitHub README fallbacks (the docs-site documents were unreachable from the build sandbox at label time); the corpus in this run is the real docs-site `llms-full.txt` documents (Prisma 5.98 MB, OpenAI 4.77 MB, Nuxt 4.79 MB, and others) — different documents than the labels describe. A correct answer drawn from an `llms-full.txt` section cannot match a label written against a README heading path except by accident. | MEASURED 2026-09-17 on PAR-653 (Linear comment). Gold-set provenance metadata (`docs/eval/probe-gold.json`) confirms the label/corpus mismatch. | **ACCEPT-WITH-RISK** — ship 0.2.0 making no retrieval-quality claim rather than block the release on re-labelling. | Verified 2026-09-17: `grep -rniE "better (ranking\|retrieval\|results)\|improve[sd]? (ranking\|retrieval\|relevance)\|more relevant" README.md docs/` returns nothing — 0.2.0's text makes no such claim. PAR-658's done-when (BM25 measurably improves top-1) stays **unmet** and is accepted as such for this release; it cannot be honestly judged until the gold set matches the real corpus. Filed as **PAR-827** (0.2.1 milestone): re-label all 60 probe questions against the real docs-site documents, then re-run the eval and post the result to PAR-658. |
| **Coverage on the built-in top-30, measured against real docs sites, is 24/30 (80%) healthy — not 30/30.** `vibectx doctor`, 2026-09-17T22:54:29.291Z, `main` @ `6de98f8`, cache `~/.vibectx`. Six unhealthy: `next.js`, `shadcn`, `hono`, `clerk`, `motion` are index-only primaries with zero links followed despite links being available (`next.js` 0/3, `shadcn` 0/5, `hono` 0/52, `motion` 0/10 — followed/dropped), and `tailwindcss` (a `readme`-kind source) misses one of its two probe queries ("responsive breakpoints": no match). Full table below. | MEASURED 2026-09-17 on PAR-653 (Linear comment); machine-readable form in `doctor --json`. | **ACCEPT** — ship with the honest 24/30 figure; `doctor` now correctly marks the index-only-zero-links-followed shape unhealthy (A19/PAR-728 working as intended) rather than reporting these libraries as covered. | No revisit date set; `doctor`'s per-library diagnosis (why a source is index-only vs. why its links were dropped) is tracked separately — see PAR-653's own comment thread for the per-library root-cause reasoning (HTML-following limits, wrong source URL, undiagnosed link-extraction gaps). |
| **The declared floor `engines.node >=20.19.0` (D-75, PAR-778) is NOT exercised at its minimum.** The test suite and `npm audit` above both ran on Node v26.0.0; CI (`.github/workflows/ci.yml`) proves Node 22. Nothing in this release's evidence has run the suite at 20.19.x itself — the floor is asserted (it matches what `vitest`'s `vite` dependency declares it needs) rather than demonstrated on that exact version. | MEASURED 2026-09-17: `node --version` on every machine that produced this record's numbers reports v26.0.0; `.github/workflows/ci.yml`'s `node-version: 22`. | **ACCEPT** — the floor is a documented requirement drawn directly from `vite`'s own declared range (D-75), not a number chosen without basis; ship without a dedicated 20.19.x CI leg for this release. | Add a CI matrix leg pinned to `20.19.0` exactly (the floor's own value, not merely "20.x") whenever the cost of a second CI job is worth closing this gap — no date forced. |
| **`npm audit` on the full tree (including dev dependencies) reports 2 moderate advisories, both in the `vitest`/`@vitest/mocker` dev-dependency chain** (`GHSA-82fw-gwwq-j7x9`, a path-traversal/arbitrary-file-read issue in `@vitest/mocker`'s redirect-mock handling). `npm audit --omit=dev` — the dependency set that actually ships to and is installed by a consumer — reports 0 vulnerabilities, confirming these two are absent from anything a consumer installs. | MEASURED 2026-09-17, `main` @ `032549b`: `npm audit` → 2 moderate (vitest chain only); `npm audit --omit=dev` → 0 vulnerabilities. | **ACCEPT** — a dev-only test-runner dependency's advisory never reaches a consumer's install; fixing it (`npm audit fix --force`) would force a breaking `vitest@5` upgrade with no corresponding security benefit to anyone running this package. | Revisit on the next routine dev-dependency bump, not on its own schedule — this is not a release-blocking exposure. |

### Full doctor table — MEASURED 2026-09-17T22:54:29.291Z, `main` @ `6de98f8`, cache `/Users/blackraptorai/.vibectx` (from PAR-653)

```
library         kind        cache  probe                                   links  mark
next.js         index-only  0.0h   2 probes: 2 answered                    0/3    ✗
react           index-only  0.0h   2 probes: 2 index-followed              6/0    ✓
supabase        index-only  0.0h   2 probes: 2 index-followed              2/0    ✓
tailwindcss     readme      0.0h   2 probes: 1 answered, 1 no match        0/0    ✗
shadcn          index-only  0.0h   2 probes: 2 answered                    0/5    ✗
stripe          index-only  0.0h   2 probes: 2 index-followed              10/1   ✓
ai-sdk          full-text   0.0h   2 probes: 2 answered                    0/0    ✓
expo            index-only  0.0h   2 probes: 2 index-followed              10/0   ✓
drizzle-orm     full-text   0.0h   2 probes: 2 answered                    0/0    ✓
prisma          full-text   0.0h   2 probes: 2 answered                    0/0    ✓
trpc            full-text   0.0h   2 probes: 2 answered                    0/0    ✓
zod             full-text   0.0h   2 probes: 2 answered                    0/0    ✓
hono            index-only  0.0h   2 probes: 2 answered                    0/52   ✗
bun             full-text   0.0h   2 probes: 2 answered                    0/0    ✓
vite            full-text   0.0h   2 probes: 2 answered                    0/0    ✓
clerk           index-only  0.0h   2 probes: 2 no match                    0/0    ✗
convex          index-only  0.0h   2 probes: 1 answered, 1 index-followed  10/88  ✓
firebase        readme      0.0h   2 probes: 2 answered                    0/0    ✓
openai          full-text   0.0h   2 probes: 2 answered                    0/0    ✓
anthropic-sdk   index-only  0.0h   2 probes: 2 index-followed              10/0   ✓
playwright      readme      0.0h   2 probes: 2 answered                    0/0    ✓
vitest          full-text   0.0h   2 probes: 2 answered                    0/0    ✓
react-router    readme      0.0h   2 probes: 2 answered                    0/0    ✓
astro           readme      0.0h   2 probes: 2 answered                    0/0    ✓
sveltekit       full-text   0.0h   2 probes: 2 answered                    0/0    ✓
nuxt            full-text   0.0h   2 probes: 2 answered                    0/0    ✓
vue             full-text   0.0h   2 probes: 2 answered                    0/0    ✓
tanstack-query  index-only  0.0h   2 probes: 2 index-followed              2/0    ✓
motion          index-only  0.0h   2 probes: 2 answered                    0/10   ✗
resend          full-text   0.0h   2 probes: 2 answered                    0/0    ✓

24/30 libraries healthy
✗ next.js: index-only, no links followed (answered from the link list at best)
✗ tailwindcss: no match: "responsive breakpoints"
✗ shadcn: index-only, no links followed (answered from the link list at best)
✗ hono: index-only, no links followed (answered from the link list at best)
✗ clerk: index-only, no links followed (answered from the link list at best); no match: "middleware protect routes"; no match: "useUser hook"
✗ motion: index-only, no links followed (answered from the link list at best)
```

**Reading of it (from PAR-653):** five of the six failures share one shape — an index-only
primary where zero links were followed while links were available to follow (`hono` skipped
52, `motion` 10, `shadcn` 5, `next.js` 3). Those libraries answer from a link list rather than
prose; that is the PAR-704 failure class, and `doctor` now marks it ✗ rather than reporting
healthy. Why those specific links are skipped is not diagnosed in this record.

## What DID verify against real data (2026-09-17, from PAR-653)

- **`vibectx warm` resolved 10 of 10 unfamiliar dependencies in a real project**
  (`/Volumes/BlackRaptorAI_PROJECTS/AA_PARAGON_PLATFORM`, manifest `package.json`) — none of the
  eleven dependencies checked were registry entries, so every one went through
  `resolve_library`; 10 resolved and cached, 1 denied by the noise list
  (`@types/html-to-text`).
- **A11's version fallback (D-50) was observed live, not just in a fixture.** Two of the ten
  resolutions hit the "no versioned document found" path and fell back to latest, stating so
  non-silently: `pdf-parse: no versioned document found for 1.1.1; latest cached instead` and
  `@biomejs/biome: no versioned document found for 2.5.14; latest cached instead`. This is
  D-50's rule ("the fallback to latest is always stated, never silent") behaving exactly as
  specified, on a real manifest.

## 7. Sign-off

> I ran the applicable gate reviews (per PR) and take responsibility for the risk acceptances
> recorded above.

**Signed:** ______________  **Date:** ___________
